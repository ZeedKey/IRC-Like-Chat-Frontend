overwrite: true
schema: "http://localhost:3000/graphql"
documents: "src/schemas/**/*.graphql"
generates:
  src/schemas/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
